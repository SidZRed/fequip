#ifndef DECRYPT_H
#define DECRYPT_H

#include <bits/stdc++.h>
#include <sstream>

using namespace std;

// Function declarations
int decryptFile(string inputFile, string outputFile, string keyFile, string type);

#endif // DECRYPT_H
