#ifndef ENCRYPT_H
#define ENCRYPT_H

#include <bits/stdc++.h>
#include <sstream>

using namespace std;

// Function declarations
int encryptFile(string inputFile, string outputFile, string keyFile, string type);

#endif // ENCRYPT_H
